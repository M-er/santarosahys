import { ElementRef } from '@angular/core';
import { MatSnackBar } from '@angular/material';
export declare class CuitDirective {
    private el;
    private snackBar;
    constructor(el: ElementRef, snackBar: MatSnackBar);
    private regex;
    private specialKeys;
    onKeyDown(event: any): void;
}
